{
  "name": "telegram-triage-agent",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply telegram-agent-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply telegram-agent-db --remote"
  },
  "dependencies": {
    "@ai-sdk/openai": "^3.0.53",
    "@ai-sdk/openai-compatible": "^2.0.41",
    "ai": "^6.0.168",
    "hono": "^4.7.0",
    "workers-ai-provider": "^3.1.12"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250401.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.14.0"
  }
}
